<h1>New Cycle - Incident Objectives</h1>

<div class="control-line">
	<strong>Incident:</strong> <span><%= @incident.name %></span>
</div>

<%= render 'form' %>

<%= link_to 'Back', incident_cycles_path(@incident) %>
