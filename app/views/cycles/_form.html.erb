<%= simple_form_for([@incident, @cycle]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :from, as: :datetime %>
    <%= f.input :to, as: :datetime %>
    <%= f.input :number, as: :integer %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
